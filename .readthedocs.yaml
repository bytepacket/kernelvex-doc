version: 2

build:
  os: ubuntu-lts-latest
  tools:
    nodejs: latest
  jobs:
    install:
      - npm i
    build:
      html:
        - RTD_BUILD=true npm run build
        - mkdir --parents $READTHEDOCS_OUTPUT/html/
        - cp --recursive out/* $READTHEDOCS_OUTPUT/html/